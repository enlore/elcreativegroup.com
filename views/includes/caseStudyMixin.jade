mixin caseStudyBlock(href, imgSrc, title, tags)
  if href
    .gig
      .row
        .col-md-6
          img.img-responsive(src=imgSrc)

        .col-md-6
          .copy
            h2= title
            p.categories= tags.join(" / ")
            hr
            if block
              block

            if href
              a(href=href, target="_blank")
                p.hint Click to see more
  else
    .gig
      .row
        .col-md-6
          img.img-responsive(src=imgSrc)

        .col-md-6
          .copy
            h2= title
            p.categories= tags.join(" / ")
            hr
            if block
              block

            if href
              p.hint Click to see more

        //button.reveal.btn-elcg.btn.cta Click to see more&hellip;
